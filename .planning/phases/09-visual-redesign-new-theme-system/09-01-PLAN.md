# Plan 09-01: Visual Redesign — New Theme System

**Phase:** 9  
**Date:** 2026-03-17  
**Goal:** Replace all CSS/themes with flat/brutalist design system from prompt.txt; add WavyBackground to Hero.

## Steps

### Step 1: Install simplex-noise
```bash
cd moduly-ai-landing && npm install simplex-noise
```
**Done when:** `package.json` has `simplex-noise` in dependencies, `node_modules/simplex-noise` exists

---

### Step 2: Update index.html — Google Fonts
Replace Inter font link with DM Sans + Space Mono. Keep Material Icons link.

**Done when:** `index.html` imports `DM+Sans` and `Space+Mono` from fonts.googleapis.com

---

### Step 3: Rewrite index.css — New Design System
Replace `:root` and `[data-theme="dark"]` blocks with:
- New CSS variables (`--primary`, `--secondary`, `--accent`, `--background`, etc.)
- Bridge aliases (`--color-primary: var(--primary)`, etc.) so per-page CSS keeps working
- New `--font-family: var(--font-sans)` pointing to DM Sans
- New shadow vars: hard flat shadows (`4px 4px 0px #000`)
- Border radius: `--border-radius-*: 0px`
- Update `.card`, `.btn`, `.btn-primary`, `.btn-secondary`, `.badge`, `.gradient-text`, `.divider`, `.icon-container` to use brutalist style (solid borders, hard shadows, no gradient backgrounds for main elements)

**Done when:** New vars in `:root`, dark mode in `[data-theme="dark"]`, bridges present, global components updated

---

### Step 4: Create wavy-background.tsx
Create `src/components/ui/wavy-background.tsx`:
- Remove `"use client"`
- Remove `import { cn }`
- Replace Tailwind classes with custom CSS classes: `wavy-bg-container`, `wavy-bg-canvas`, `wavy-bg-content`
- Fix `children?: any` → `children?: React.ReactNode`
- Fix `animationId` scoping (move to useRef or inside useEffect)
- Add `import type` for type-only imports

**Done when:** File compiles cleanly with `tsc --noEmit`

---

### Step 5: Integrate WavyBackground into Hero.tsx + update Hero.css
- `Hero.tsx`: import `WavyBackground`, replace `.hero-bg` div with `<WavyBackground>` as background layer, pass theme-appropriate colors
- `Hero.css`: 
  - Remove gradient/pattern bg styles (handled by WavyBackground canvas)
  - Keep `.hero-container`, `.hero-content`, stats, visual card
  - Update `.visual-card` to flat brutalist: solid `2px` border, hard shadow, `0px` radius
  - Update `.hero-badge` to brutalist pill → solid black border box
  - `.title-main`: remove gradient text, use solid `--primary` color
  - `.stat-number`: use `--primary` color

**Done when:** Hero renders with animated canvas waves behind content, no z-index conflicts

---

### Step 6: Rewrite AuthModal.css
Replace 886-line glassmorphism with flat brutalist (~150 lines):
- `.auth-overlay`: solid dark backdrop (no blur)
- `.auth-modal`: solid `var(--card)` bg, `2px solid var(--border)`, `4px 4px 0px var(--border)` box-shadow, `0px` radius
- All inputs: solid border, `0px` radius, no blur
- Buttons: flat fill with hard shadow
- Dark mode via `[data-theme="dark"]`

**Done when:** AuthModal has minimal brutalist CSS, no backdrop-filter, no glassmorphism

---

### Step 7: Sweep Section CSS files
For each of: `Header.css`, `Footer.css`, `Features.css`, `HowItWorks.css`, `Modes.css`, `Solution.css`, `Problem.css`, `TechStack.css`, `Scope.css`, `Comparison.css`, `CTA.css`:
- Replace `backdrop-filter: blur(...)` with `background: var(--background)`
- Replace gradient fills on cards/buttons → solid fill + hard shadow
- Replace `border-radius: var(--border-radius-*)` → keep using var (bridge now points to 0px)
- Replace `linear-gradient(...)` backgrounds with solid `var(--primary)` or `var(--secondary)` fills
- Update `box-shadow` to hard shadows where appropriate

**Done when:** No glassmorphism remaining in section files, all vars updated

---

### Step 8: Sweep Per-Page CSS files
For each of: `StudyMode.css`, `ExamMode.css`, `Library.css`, `Dashboard.css`, `UploadDocs.css`, `Onboarding.css`, `Settings.css`, `AppNav.css`, `ThemeToggle.css`:
- Same sweep: remove blur, flatten gradients, use solid borders + hard shadows
- Keep structural CSS intact (flex, grid, sizing)

**Done when:** No glassmorphism/blur remaining in page files

---

### Step 9: TypeScript checks
```bash
npx tsc --noEmit
npx tsc -p tsconfig.functions.json --noEmit
```

**Done when:** Both checks return 0 errors

---

## Acceptance Criteria
- [ ] `simplex-noise` installed and WavyBackground compiles
- [ ] Hero section shows animated wave background
- [ ] All pages use flat/brutalist style (no glassmorphism, no blurs)
- [ ] Dark mode toggle correctly inverts theme (black bg, white border, etc.)
- [ ] Both TS checks pass with 0 errors
- [ ] No regressions in chat/exam/library functionality
